import React from "react";
import { Marker, useMap } from "react-leaflet";

export const LocationMarker = () => {
    return (
        <React.Fragment>
            
        </React.Fragment>
    );
};

